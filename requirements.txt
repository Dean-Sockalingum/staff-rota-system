# Core Django
Django>=4.2,<5.0
djangorestframework
django-cors-headers
python-decouple

# Celery for background tasks
celery
django-celery-beat
redis

# Communications
twilio

# Phase 2: PDF/Excel Export
WeasyPrint>=60.0  # PDF generation from HTML/CSS
openpyxl>=3.1.0  # Excel file generation

# Phase 2: Advanced Search (Elasticsearch)
elasticsearch>=7.17.0,<8.0.0  # Elasticsearch client
elasticsearch-dsl>=7.4.0,<8.0.0  # High-level Elasticsearch DSL
django-elasticsearch-dsl>=7.3.0  # Django integration for Elasticsearch

# Phase 2: Email & Calendar
django-anymail[mailgun]>=10.0  # Email backend
icalendar>=5.0.0  # iCal/ICS calendar export

# Phase 6: Security Hardening Dependencies
django-axes>=6.1.0  # Brute force protection & account lockout
django-auditlog>=2.3.0  # Comprehensive audit logging
django-otp>=1.3.0  # Two-factor authentication (2FA) support
qrcode>=7.4.2  # QR code generation for 2FA setup
django-encrypted-model-fields>=0.6.5  # Field-level encryption
django-csp>=3.7  # Content Security Policy headers
safety  # Dependency vulnerability scanner
pip-audit  # Python package auditing
urllib3>=2.6.0  # HTTP library (CVE-2025-66418, CVE-2025-66471 fixed)
sentry-sdk>=1.40.0  # Error tracking and performance monitoring (Task 51)

# Phase 6: Machine Learning Dependencies
prophet>=1.1.5  # Facebook's time series forecasting
scikit-learn>=1.3.0  # Machine learning library
pandas>=2.0.0  # Data manipulation
numpy>=1.24.0  # Numerical computing
matplotlib>=3.7.0  # Plotting (for model diagnostics)
seaborn>=0.12.0  # Statistical visualization
PuLP>=2.7.0  # Linear programming for optimization

# Performance Monitoring
psutil>=5.9.0  # System and process utilities for performance benchmarks

# AI Assistant
fuzzywuzzy>=0.18.0  # Fuzzy string matching for name search
python-Levenshtein>=0.21.0  # Fast string matching (optional speedup for fuzzywuzzy)

# Care Inspectorate Integration
beautifulsoup4>=4.12.0  # HTML parsing for CI website
requests>=2.31.0  # HTTP requests for report fetching
python-dateutil>=2.8.0  # Date manipulation for ML analysis
